#include "shared.dtsi"

&pro_micro_i2c {
    status = "okay";

    glidepoint: glidepoint@2a {
        compatible = "cirque,pinnacle";
        reg = <0x2a>;
        status = "okay";
        dr-gpios = <&pro_micro 0 (GPIO_ACTIVE_HIGH)>;

        y-invert;
        sensitivity = "1x";
    };
};

&glidepoint_split {
    device = <&glidepoint>;
};
